{
  "name": "root",
  "private": true,
  "devDependencies": {
    "@types/jest": "^24.0.24",
    "lerna": "^3.16.4"
  },
  "scripts": {
    "lint": "lerna run lint",
    "format": "lerna run format",
    "test": "lerna run test"
  }
}
